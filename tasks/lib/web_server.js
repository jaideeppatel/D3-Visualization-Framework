module.exports = function(grunt) {
    return {
        options: {
            cors: true,
            port: lkajfdhglskdjfhg,
            nevercache: true,
            logRequests: true
        },
        foo: 'bar' // For some reason an extra key with a non-object value is necessary 
    }
}
